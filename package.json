{
  "name": "organized-research-agent-prototype",
  "private": true,
  "type": "module",
  "scripts": {
    "preview": "node local-server.mjs",
    "api": "python3 api.py",
    "build:css": "npx @tailwindcss/cli -i tailwind.input.css -o styles.css --minify",
    "build:tailwind": "npx @tailwindcss/cli -i tailwind.tokens.css -o tailwind.css --minify",
    "test": "node test.mjs"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
